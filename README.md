# SmartInvest

A simple Streamlit project for analyzing investment portfolios.

## Run locally

1. Install dependencies:
   `pip install -r requirements.txt`
2. Start the app:
   `streamlit run app.py`

## Notes

The app supports local CSV export for the generated analysis report.
